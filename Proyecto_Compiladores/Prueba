void inicio(void)
{
	switch(var)
	{
		case 3:
		int var=0;
		float var=-3.5f;
		break;
		default:
		break;
	}
	while(al>=ab || aa==bb && b==b)
	{
	int variable = b+a;
	}
	//printf;
	for(int abc=3; abc>4; abc++)
	{
		if(calabozo==calabozo)
		{
		//no hagas nada
		}
		if(ab!=bc){
		p=5*16-v+w;
		} else if(p==0){
		abc=3+1;
		}else{
		printf("cualquier cosa");
		}
	}
	printf("mensaje de error.");
}
